"""智能教室仿真应用的业务模块包。

这个包把原本集中在 `app.py` 中的逻辑拆成更清晰的层次：

- `models`：数据结构与类型定义；
- `physics`：热平衡、CO₂ 守恒、功耗等物理模型；
- `control`：MPC 候选动作评估与传统基准控制器；
- `state`：Streamlit 会话状态的初始化、重置与单步推进；
- `sidebar` / `views`：页面输入与展示。
"""

